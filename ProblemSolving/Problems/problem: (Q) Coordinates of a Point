#include <iostream>
using namespace std;
#include <cctype>
int main() {
    float x;
    float y;
    cin >> x >> y;
    if (x > 0 && y > 0) {
        cout << "Q1";
    }
   else if (x < 0 && y > 0) {
       cout << "Q2";
   }
   else if (x < 0 && y < 0) {
       cout << "Q3";
   }
   else if (x > 0 && y < 0) {
       cout << "Q4";
   }
   else if ((y < 0 || y > 0) && x == 0) {
       cout << "Eixo Y";
   }
   else if ( (x < 0 || x > 0) && y == 0) {
       cout << "Eixo X";
   }
    else {
        cout << "Origem";
    }
 
}
